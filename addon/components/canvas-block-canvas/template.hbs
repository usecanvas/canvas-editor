<div local-class="icon"></div>

<div local-class="content">
  <a local-class="title" href="{{unfurl.id}}">{{unfurl.title}}</a>
  <div local-class="summary">{{unfurl.text}}</div>
</div>

{{#if hasProgress}}
  <div local-class="progress">
    {{ui-progress-donut progress=progress}}
  </div>
{{/if}}
