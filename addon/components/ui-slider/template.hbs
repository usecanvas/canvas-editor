<input
  type="range"
  min={{min}}
  max={{max}}
  value={{value}}
  local-class="slider"
  onchange={{action 'updateValue' value='target.value'}}
  oninput={{action 'updateValue' value='target.value'}}>
