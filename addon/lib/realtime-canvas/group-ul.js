import Group from './group';
/**
 * A group representing a unordered-list.
 *
 * @class CanvasEditor.RealtimeCanvas.GroupUL
 * @extends CanvasEditor.RealtimeCanvas.Block
 */
export default Group.extend({
  type: 'unordered-list',
});
