import ContentBlock from './content-block';

/**
 * A block representing a RunKit.
 *
 * @class CanvasEditor.RealtimeCanvas.RunKit
 * @extends CanvasEditor.RealtimeCanvas.ContentBlock
 */
export default ContentBlock.extend({
  isCard: true,
  type: 'runkit'
});
